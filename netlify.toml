[build]
  # Clean install + production build
  command   = "npm ci && npm run build"
  publish   = "dist/anamolydetector"

[build.environment]
    # Pin Node if you haven’t yet:
  NODE_VERSION  = "16"
  # Work around the OpenSSL3 initialization error:
 

